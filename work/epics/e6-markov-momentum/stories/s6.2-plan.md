---
story_id: "S6.2"
epic_id: "E6"
title: "S6.2: Inertia xG Modifier plan"
status: "active"
created: "2026-06-14"
---

# Plan: S6.2: Inertia xG Modifier

## Overview
- **Story ID:** S6.2
- **Size:** S (3 SP)
- **Target Date:** 2026-06-14

## Gate Policy

| When | Command | Scope |
|------|---------|-------|
| Per task | `rai gate check gate-tests --scope Oloraculo.Web.Tests/Predictors/` | Only tests for the changed module (Predictors tests) |
| Per task | `rai gate check gate-lint` | Full project (fast) |
| Per task | `rai gate check gate-format` | Full project (fast) |
| Per task | `rai gate check gate-types` | Full project |
| End of story | `rai gate check gate-tests --scope Oloraculo.Web.Tests/` | Changed package only — NOT full suite |
| MR creation | `/rai-mr-create` | Full suite — the ONLY place where unscoped tests run |

## Tasks

### Task 1: Implement expected goals adjustment in GoalPlusRecentContextModel
- **Description:** Implement the momentum-based expected goals multiplier in `GoalPlusRecentContextModel.Predict` and add unit tests to `Oloraculo.Web.Tests/Predictors/GoalPlusRecentContextModelTests.cs` using TDD.
- **Files to create/modify:**
  - Modify: `Oloraculo.Web/Predictors/GoalPlusRecentContextModel.cs`
  - Create: `Oloraculo.Web.Tests/Predictors/GoalPlusRecentContextModelTests.cs`
- **TDD Steps:**
  1. Write failing unit tests for:
     - Multiplier computation from non-degraded MarkovWin transition probabilities (e.g. 0.60 Win probability maps to 1.10 multiplier, and 0.20 Win probability maps to 0.96 multiplier).
     - Fallback to 1.00 when `MarkovMomentumPredictor.Predict` is degraded.
     - Correct logging of `Drivers`, `FeaturesUsed`, and `Explanation` when momentum adjustment is active or degraded.
  2. Implement the integration in `GoalPlusRecentContextModel.cs`.
  3. Ensure all tests pass.
- **AC Reference:** AC-001, AC-002, AC-003, AC-004.
- **Verification:**
  - `rai gate check gate-tests --scope Oloraculo.Web.Tests/Predictors/`
  - `rai gate check gate-lint`
  - `rai gate check gate-format`
  - `rai gate check gate-types`
- **Size:** S
- **Dependencies:** None

### Task 2: End-to-End manual integration validation
- **Description:** Write an integration-level xUnit test or run a test scenario to verify that `GoalPlusRecentContextModel` correctly adjusts expected goals when loaded through the `PredictionService` and the simulation pipeline.
- **Files to modify:** None (add integration test to `Oloraculo.Web.Tests/Predictors/GoalPlusRecentContextModelTests.cs`).
- **TDD Steps:**
  - Write an integration test setting up a full simulated tournament context (where home and away team have sufficient history).
  - Validate that the simulation results correctly apply the `GoalPlusRecentContextModel` predictions containing the adjusted expected goals.
- **Verification:**
  - `rai gate check gate-tests --scope Oloraculo.Web.Tests/Predictors/`
- **Size:** XS
- **Dependencies:** Task 1

## Execution Order
1. **Task 1** (Foundation): Implement the multiplier math, the predictor instantiation, and verify behavior with unit tests.
2. **Task 2** (Verification): Write integration tests to check predictions end-to-end within the simulation pipeline.

## Risks and Mitigations

| Risk | Likelihood/Impact | Mitigation |
|------|:-----------------:|------------|
| Multiplier calculations throwing divide-by-zero or nan | L/H | Standardize default baseline (1/3) and use defensive C# clamping math (`Math.Clamp`). |
| Circular reference between GoalPlusRecentContextModel and MarkovMomentumPredictor | L/M | The predictors do not reference each other at the class level; `GoalPlusRecentContextModel` only calls `MarkovMomentumPredictor` as a helper. |

## Duration Tracking

| Task | Estimated | Actual | Completed Date |
|------|-----------|--------|----------------|
| Task 1 | 2 hours | - | - |
| Task 2 | 0.5 hours | - | - |
